{
  "name": "dark-tower-tattoo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev --hostname localhost --port 3001",
    "clean": "rm -rf .next",
    "dev:clean": "rm -rf .next && next dev --hostname localhost --port 3001",
    "build": "next build",
    "start": "next start --hostname localhost --port 3001",
    "preview": "next build && next start --hostname localhost --port 3001"
  },
  "dependencies": {
    "framer-motion": "^12.38.0",
    "lucide-react": "^1.8.0",
    "next": "^15.3.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
